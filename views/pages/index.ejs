<h1>Home</h1>
<a href="/new">New</a>
<ul>
  <% messages.forEach(({id, user, message, date}) => { %>
    <li>
      <p><%= user %></p>
      <p><%= message %></p>
      <p><%= date %></p>
      <a href="message/<%= id %>">Open</a>
    </li>
  <% }) %>
</ul>
